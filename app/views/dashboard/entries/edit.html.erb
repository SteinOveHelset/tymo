<h1 class="mb-6 text-3xl text-[#989898]">Edit tracked time</h1>

<%= form_with model: @entry, url: dashboard_entry_path(@entry), class: "space-y-4" do |form| %>
    <%= render 'form', entry: @entry, form: form %>
<% end %>